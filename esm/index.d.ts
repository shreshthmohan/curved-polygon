export declare function roundedPolygonBySideLength({ sideLength, sideCount, borderRadius, cx, cy, }: {
    sideLength: number;
    sideCount: number;
    borderRadius?: number;
    cx?: number;
    cy?: number;
}): string;
export declare function roundedPolygonByCircumRadius({ circumRadius, sideCount, borderRadius, cx, cy, }: {
    circumRadius: number;
    sideCount: number;
    borderRadius?: number;
    cx?: number;
    cy?: number;
}): string;
